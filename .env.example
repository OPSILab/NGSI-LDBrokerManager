# NGSI-LD Broker Manager standalone stack — environment template.
# Copy to `.env` (git-ignored) and fill in real values.

SERVER_DEBUG=false
IDRA_BASE_PATH=http://your-idra-host:8080
HOST_ORION=http://your-orion-host
PORT_ORION=1026
